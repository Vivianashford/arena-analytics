import { SettingsClient } from '@/features/arena/components/settings-client';

export const metadata = {
  title: 'Arena Analytics : Settings'
};

export default function SettingsPage() {
  return <SettingsClient />;
}
